% compresion

# Extract the files from a tar
tar xf <tar_file>

# Extract targz
tar xzf <tar_gz_file>

# Create zip file
zip -r <output>.zip <files>

# create a zip with compression level
zip -r -<compression_level> <output>.zip <files>

$ tar_file: ls *.tar
$ tar_gz_file: ls *.tar.gz
$ files: find . -mindepth 1 -maxdepth 2 -print --- --multi --expand
$ compression_level: echo "1 2 3 4 5 6 7 8 9" | tr ' ' '\n'
